---
phase: 08-polish-app-store-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/userRoutes.ts
  - mobile/lib/src/features/profile/presentation/settings_screen.dart
  - mobile/lib/src/features/profile/data/account_repository.dart
  - mobile/lib/src/features/profile/presentation/providers/profile_providers.dart
  - mobile/lib/src/features/profile/presentation/providers/profile_providers.g.dart
  - mobile/ios/Runner/PrivacyInfo.xcprivacy
  - mobile/ios/Runner.xcodeproj/project.pbxproj
  - backend/src/scripts/seed-demo.ts
  - backend/package.json
  - mobile/.fvmrc
autonomous: true

must_haves:
  truths:
    - "User can request account deletion from Settings screen"
    - "User sees 30-day grace period explanation before confirming deletion"
    - "User is logged out after requesting deletion"
    - "User can cancel pending deletion by logging back in (future: cancel-deletion endpoint)"
    - "App includes PrivacyInfo.xcprivacy in iOS bundle"
    - "Demo account exists with realistic check-in data for App Store review"
    - "Flutter version is pinned via .fvmrc to a version >= 3.27"
  artifacts:
    - path: "backend/src/routes/userRoutes.ts"
      provides: "Account deletion API routes"
      contains: "delete-account"
    - path: "mobile/lib/src/features/profile/presentation/settings_screen.dart"
      provides: "Delete Account UI in settings"
      contains: "Delete Account"
    - path: "mobile/lib/src/features/profile/data/account_repository.dart"
      provides: "Account deletion API client"
      contains: "deleteAccount"
    - path: "mobile/ios/Runner/PrivacyInfo.xcprivacy"
      provides: "iOS privacy manifest declaring collected data and required reason APIs"
      contains: "NSPrivacyAccessedAPITypes"
    - path: "backend/src/scripts/seed-demo.ts"
      provides: "Demo account seed script with realistic data"
      contains: "demo@soundcheck.app"
    - path: "mobile/.fvmrc"
      provides: "Flutter version pin"
      contains: "flutter"
  key_links:
    - from: "mobile/lib/src/features/profile/presentation/settings_screen.dart"
      to: "backend/src/routes/userRoutes.ts"
      via: "POST /api/users/me/delete-account"
      pattern: "delete-account"
    - from: "backend/src/routes/userRoutes.ts"
      to: "backend/src/services/DataRetentionService.ts"
      via: "DataRetentionService.requestAccountDeletion()"
      pattern: "requestAccountDeletion"
    - from: "mobile/ios/Runner/PrivacyInfo.xcprivacy"
      to: "mobile/ios/Runner.xcodeproj/project.pbxproj"
      via: "Copy Bundle Resources build phase"
      pattern: "PrivacyInfo.xcprivacy"
---

<objective>
Add Apple App Store compliance requirements: account deletion flow, privacy manifest, Flutter version pin, and demo account for review.

Purpose: Apple requires (1) working account deletion (not just deactivation), (2) privacy manifests declaring Required Reason API usage, (3) no Flutter 3.24.3/3.24.4 builds (App Store rejection bug), and (4) a demo account for App Store reviewers. The backend DataRetentionService already implements full GDPR-compliant deletion with 30-day grace period -- this plan wires it to API routes and mobile UI.

Output: Working account deletion flow, PrivacyInfo.xcprivacy in iOS bundle, .fvmrc pinning Flutter, demo account seed script.
</objective>

<execution_context>
@C:\Users\dasbl\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dasbl\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-app-store-readiness/08-RESEARCH.md
@backend/src/services/DataRetentionService.ts
@backend/src/routes/userRoutes.ts
@mobile/lib/src/features/profile/presentation/settings_screen.dart
@mobile/lib/src/features/profile/presentation/providers/profile_providers.dart
@backend/src/scripts/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Account deletion API routes and mobile UI</name>
  <files>
  backend/src/routes/userRoutes.ts
  mobile/lib/src/features/profile/data/account_repository.dart
  mobile/lib/src/features/profile/presentation/settings_screen.dart
  mobile/lib/src/features/profile/presentation/providers/profile_providers.dart
  mobile/lib/src/features/profile/presentation/providers/profile_providers.g.dart
  </files>
  <action>
  **Step A: Backend -- Add deletion routes to userRoutes.ts**

  Add three routes to userRoutes.ts, placed AFTER `router.delete('/me', ...)` and BEFORE the device-token routes. Import DataRetentionService at the top:

  ```typescript
  import { DataRetentionService } from '../services/DataRetentionService';
  const dataRetentionService = new DataRetentionService();
  ```

  Routes to add:
  1. `POST /me/delete-account` -- calls `dataRetentionService.requestAccountDeletion(userId)`, returns the result. On error (user not found, existing request), return 400 with error message.
  2. `POST /me/cancel-deletion` -- calls `dataRetentionService.cancelDeletionRequest(userId)`, returns success. On error (no pending request), return 400 with error message.
  3. `GET /me/deletion-status` -- calls `dataRetentionService.getDeletionRequestStatus(userId)`, returns the status or `{ data: null }` if no request exists.

  All three routes use `authenticateToken` middleware. Follow the existing route handler pattern (try/catch with next(error), response format `{ success: true, data: result }`).

  **Step B: Mobile -- Create AccountRepository**

  Create `mobile/lib/src/features/profile/data/account_repository.dart`:
  - Class `AccountRepository` that takes `DioClient` (from core/network/dio_client.dart)
  - Method `Future<Map<String, dynamic>> requestAccountDeletion()` -- POST to `/api/users/me/delete-account`
  - Method `Future<void> cancelDeletion()` -- POST to `/api/users/me/cancel-deletion`
  - Follow existing repository patterns (e.g., upload_repository.dart or profile repository)

  **Step C: Mobile -- Add Delete Account to SettingsScreen**

  In settings_screen.dart:
  1. Add import for `account_repository.dart` and the core DioClient provider.
  2. In the Account section, add a "Delete Account" tile ABOVE the existing Logout tile:
     ```dart
     _SettingsTile(
       title: 'Delete Account',
       subtitle: 'Permanently remove your account and data',
       leading: const Icon(Icons.delete_forever, color: AppTheme.error),
       textColor: AppTheme.error,
       onTap: () => _showDeleteAccountDialog(context, ref),
     ),
     ```
  3. Create a `_showDeleteAccountDialog` static/top-level function that shows an AlertDialog with:
     - Title: "Delete Account?"
     - Content explaining: "Your account will be deactivated immediately and permanently deleted after 30 days. During this period, you can cancel by logging back in.\n\nThis action cannot be undone after 30 days."
     - Cancel button and a red "Delete My Account" confirmation button
     - On confirmation: call AccountRepository.requestAccountDeletion(), then logout (same pattern as existing logout), then navigate to /login
     - Show error SnackBar on failure

  Add an `accountRepositoryProvider` to profile_providers.dart:
  ```dart
  @riverpod
  AccountRepository accountRepository(Ref ref) {
    return AccountRepository(ref.watch(dioClientProvider));
  }
  ```
  Run `cd mobile && dart run build_runner build --delete-conflicting-outputs` to regenerate.

  Do NOT modify the existing Logout tile or any other settings sections.
  </action>
  <verify>
  Backend: Grep for "delete-account" in userRoutes.ts -- should find the route.
  Backend: Grep for "DataRetentionService" in userRoutes.ts -- should find import and instantiation.
  Mobile: `cd mobile && dart analyze lib/src/features/profile/` -- no errors.
  Mobile: Grep for "Delete Account" in settings_screen.dart -- should find the tile.
  Mobile: Grep for "requestAccountDeletion" in account_repository.dart -- should find the method.
  </verify>
  <done>
  Account deletion flow works end-to-end: Settings > Delete Account > Confirmation dialog > POST to backend > DataRetentionService schedules deletion > User logged out. Backend exposes three routes (request, cancel, status) wired to the existing DataRetentionService.
  </done>
</task>

<task type="auto">
  <name>Task 2: iOS privacy manifest and Flutter version pin</name>
  <files>
  mobile/ios/Runner/PrivacyInfo.xcprivacy
  mobile/ios/Runner.xcodeproj/project.pbxproj
  mobile/.fvmrc
  </files>
  <action>
  **Step A: Create PrivacyInfo.xcprivacy**

  Create `mobile/ios/Runner/PrivacyInfo.xcprivacy` with the complete privacy manifest. Use the exact template from the research document (08-RESEARCH.md "Code Examples" section) which declares:

  NSPrivacyTracking: false
  NSPrivacyTrackingDomains: empty
  NSPrivacyCollectedDataTypes:
  - PreciseLocation (linked, app functionality)
  - PhotosOrVideos (linked, app functionality)
  - CrashData (not linked, app functionality)
  - PerformanceData (not linked, app functionality)
  - EmailAddress (linked, app functionality)
  - Name (linked, app functionality)
  - UserID (linked, app functionality)

  NSPrivacyAccessedAPITypes:
  - UserDefaults (CA92.1)
  - SystemBootTime (35F9.1)
  - FileTimestamp (C617.1)

  **Step B: Add PrivacyInfo.xcprivacy to Xcode project**

  Modify `mobile/ios/Runner.xcodeproj/project.pbxproj` to include PrivacyInfo.xcprivacy in:
  1. PBXBuildFile section -- add a build file entry for PrivacyInfo.xcprivacy
  2. PBXFileReference section -- add a file reference entry
  3. PBXGroup section (Runner group children) -- add the file reference
  4. PBXResourcesBuildPhase (Copy Bundle Resources) -- add the build file reference

  Use UUIDs that follow the project's existing pattern (24-char hex strings). Look at how existing files like Info.plist or Assets.xcassets are referenced in project.pbxproj for the exact pattern to follow.

  IMPORTANT: Be careful with project.pbxproj -- it is a structured file. Only add entries, never remove or modify existing ones. Read the file first, find the exact sections, and add entries in alphabetical order within each section.

  **Step C: Create .fvmrc for Flutter version pinning**

  Create `mobile/.fvmrc`:
  ```json
  {
    "flutter": "3.27.4",
    "flavors": {}
  }
  ```

  Use 3.27.4 as a safe minimum that avoids the 3.24.3/3.24.4 App Store rejection bug (per research). The actual installed version may be higher -- this is a floor pin.

  NOTE: Do NOT install FVM or run fvm commands. Just create the .fvmrc config file. The developer will install FVM separately if desired. The .fvmrc serves as documentation of the minimum required version regardless of whether FVM is installed.
  </action>
  <verify>
  File exists: `mobile/ios/Runner/PrivacyInfo.xcprivacy` with NSPrivacyAccessedAPITypes content.
  Grep for "PrivacyInfo.xcprivacy" in project.pbxproj -- should find entries in PBXBuildFile, PBXFileReference, PBXGroup, and PBXResourcesBuildPhase.
  File exists: `mobile/.fvmrc` with flutter version >= 3.27.
  Run `cd mobile && flutter build ios --no-codesign --config-only 2>&1 | head -20` to verify Xcode project is not corrupted (should not fail on project parse).
  </verify>
  <done>
  PrivacyInfo.xcprivacy exists in ios/Runner/ and is registered in the Xcode project's Copy Bundle Resources. Flutter version is pinned to >= 3.27 via .fvmrc.
  </done>
</task>

<task type="auto">
  <name>Task 3: Demo account seed script for App Store review</name>
  <files>
  backend/src/scripts/seed-demo.ts
  backend/package.json
  </files>
  <action>
  **Step A: Create seed-demo.ts**

  Create `backend/src/scripts/seed-demo.ts` following the pattern of the existing `seed.ts` script. This script creates a demo account with realistic data for App Store review.

  The script should:
  1. Import Database, dotenv, and bcrypt (for password hashing -- check existing auth code for the bcrypt pattern used).
  2. Create or find a demo user:
     - email: `demo@soundcheck.app`
     - username: `demo_user`
     - password: `SoundCheck2026!` (hashed with bcrypt, same salt rounds as existing auth -- check UserController or auth service)
     - firstName: `Demo`
     - lastName: `User`
     - bio: `Live music enthusiast. Catch me at a show near you!`
     - is_active: true
     - Use `INSERT ... ON CONFLICT (email) DO UPDATE SET ...` to be idempotent (can re-run safely)
  3. Add an `is_demo` column guard: Run `ALTER TABLE users ADD COLUMN IF NOT EXISTS is_demo BOOLEAN DEFAULT false; UPDATE users SET is_demo = true WHERE email = 'demo@soundcheck.app';`
  4. Use existing seed venues and bands (query them from DB -- they should exist from seed.ts). Select 5-8 venues and 8-12 bands.
  5. Create 8-10 events using the demo user's city venues:
     - Events should be spread over the past 3 months (some past, some upcoming)
     - Each event has 1-3 bands in its lineup via event_lineup
     - Use `INSERT ... ON CONFLICT DO NOTHING` for idempotency
  6. Create 6-8 check-ins for past events:
     - Mix of verified and unverified location
     - Some with venue_rating (3.5-5.0 range), some without
     - Use `INSERT ... ON CONFLICT DO NOTHING` for idempotency
  7. Create 4-6 checkin_band_ratings for the check-ins (ratings 3.0-5.0)
  8. Create 2-3 follow relationships (demo user follows some seed users if they exist, or skip if no other users)
  9. Award 3-4 badges to demo user via user_badges table (pick from existing badge definitions: first_show, genre_explorer for Rock, venue_collector -- query badge_definitions for valid IDs)

  Log progress with console.log as seed.ts does. Exit cleanly.

  **Step B: Add npm script**

  In backend/package.json, add to "scripts":
  ```
  "seed:demo": "ts-node src/scripts/seed-demo.ts"
  ```

  Place it alphabetically near the existing "seed" script.

  IMPORTANT: The script must be idempotent (safe to run multiple times without creating duplicates). Use ON CONFLICT clauses everywhere.
  </action>
  <verify>
  File exists: `backend/src/scripts/seed-demo.ts` with demo@soundcheck.app.
  Grep for "seed:demo" in backend/package.json -- should find the npm script.
  Run `cd backend && npx tsc --noEmit src/scripts/seed-demo.ts 2>&1 | head -20` -- should compile without type errors (or only minor ones from missing ambient types, not structural errors).
  </verify>
  <done>
  Demo account seed script creates a realistic user (demo@soundcheck.app / SoundCheck2026!) with check-in history, badges, ratings, and follow relationships. Script is idempotent and accessible via `npm run seed:demo`. The demo user is flagged with is_demo=true for optional protection against deletion during App Store review.
  </done>
</task>

</tasks>

<verification>
1. Account deletion: POST /api/users/me/delete-account route exists and calls DataRetentionService
2. Account deletion: Settings screen shows "Delete Account" tile with confirmation dialog
3. Privacy manifest: PrivacyInfo.xcprivacy exists with Required Reason API declarations
4. Privacy manifest: Referenced in project.pbxproj Copy Bundle Resources
5. Flutter pin: .fvmrc exists with version >= 3.27
6. Demo account: seed-demo.ts creates demo@soundcheck.app with realistic data
7. All code compiles: `dart analyze` (mobile), `tsc --noEmit` (backend)
</verification>

<success_criteria>
- Account deletion works end-to-end (Settings -> Dialog -> API -> DataRetentionService -> Logout)
- PrivacyInfo.xcprivacy is included in iOS app bundle
- Flutter version pinned to safe release (>= 3.27)
- Demo account seed script creates realistic reviewer experience
- No compilation errors in either backend or mobile
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-app-store-readiness/08-02-SUMMARY.md`
</output>
