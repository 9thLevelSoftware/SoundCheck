---
phase: 07-discovery-recommendations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/EventService.ts
  - backend/src/controllers/EventController.ts
  - backend/src/routes/eventRoutes.ts
  - backend/src/routes/searchRoutes.ts
  - backend/src/utils/cache.ts
  - mobile/lib/src/features/discover/domain/discovery_models.dart
  - mobile/lib/src/features/discover/data/discovery_repository.dart
  - mobile/lib/src/features/discover/presentation/providers/discover_providers.dart
  - mobile/lib/src/features/discover/presentation/discover_screen.dart
autonomous: true

must_haves:
  truths:
    - "User can see upcoming shows near their GPS location (configurable radius and date range)"
    - "User can browse events filtered by genre"
    - "User can see trending shows near them (most check-ins in recent window)"
    - "Event search returns results by event name, band name, venue name, or genre"
    - "Discover screen pivots from band/venue discovery to event-first discovery"
  artifacts:
    - path: "backend/src/services/EventService.ts"
      provides: "getNearbyUpcoming, getTrendingNearby, getByGenre, searchEvents methods"
      contains: "getNearbyUpcoming"
    - path: "backend/src/controllers/EventController.ts"
      provides: "Controller methods for new discovery endpoints"
      contains: "getNearbyUpcoming"
    - path: "backend/src/routes/eventRoutes.ts"
      provides: "Routes for /events/discover, /events/genre/:genre, /events/search"
      contains: "discover"
    - path: "mobile/lib/src/features/discover/domain/discovery_models.dart"
      provides: "Freezed models for discovery event responses"
      contains: "DiscoverEvent"
    - path: "mobile/lib/src/features/discover/data/discovery_repository.dart"
      provides: "API client for discovery endpoints"
      contains: "DiscoveryRepository"
    - path: "mobile/lib/src/features/discover/presentation/discover_screen.dart"
      provides: "Enhanced discover screen with nearby shows, genre browse, trending"
      contains: "Nearby Shows"
  key_links:
    - from: "mobile/lib/src/features/discover/presentation/discover_screen.dart"
      to: "mobile/lib/src/features/discover/data/discovery_repository.dart"
      via: "Riverpod providers watch discoveryRepositoryProvider"
      pattern: "discoveryRepositoryProvider"
    - from: "mobile/lib/src/features/discover/data/discovery_repository.dart"
      to: "backend/src/routes/eventRoutes.ts"
      via: "Dio HTTP calls to /api/events/discover, /api/events/genre, /api/events/search"
      pattern: "/api/events/discover|/api/events/genre|/api/events/search"
    - from: "backend/src/routes/eventRoutes.ts"
      to: "backend/src/services/EventService.ts"
      via: "EventController delegates to EventService methods"
      pattern: "eventService\\.getNearbyUpcoming|eventService\\.getTrendingNearby"
---

<objective>
Build event discovery: nearby upcoming shows, genre browsing, trending events near user, and event search. Transform the discover screen from band/venue browsing into event-first discovery.

Purpose: Satisfies DISC-01 (nearby shows), DISC-06 (trending near user), DISC-07 (event search), DISC-08 (genre browsing). This is the primary discovery surface for "what show should I go to?"

Output: Five new backend endpoints for event discovery. Mobile discover screen enhanced with nearby shows, genre browse sections, trending, and event search results alongside existing band/venue/user search.
</objective>

<execution_context>
@C:\Users\dasbl\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dasbl\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-discovery-recommendations/07-RESEARCH.md

# Key source files to extend
@backend/src/services/EventService.ts
@backend/src/controllers/EventController.ts
@backend/src/routes/eventRoutes.ts
@backend/src/routes/searchRoutes.ts
@backend/src/utils/cache.ts
@mobile/lib/src/features/discover/presentation/discover_screen.dart
@mobile/lib/src/features/discover/presentation/providers/discover_providers.dart
@mobile/lib/src/core/providers/providers.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend -- Event discovery endpoints (nearby, trending, genre, search)</name>
  <files>
    backend/src/services/EventService.ts
    backend/src/controllers/EventController.ts
    backend/src/routes/eventRoutes.ts
    backend/src/routes/searchRoutes.ts
    backend/src/utils/cache.ts
  </files>
  <action>
    1. **Add cache keys** to `backend/src/utils/cache.ts` CacheKeys object:
       - `nearbyEvents: (lat: number, lon: number, radius: number) => \`events:nearby:${lat.toFixed(2)}:${lon.toFixed(2)}:${radius}\``
       - `trendingEvents: (lat: number, lon: number) => \`events:trending:${lat.toFixed(2)}:${lon.toFixed(2)}\``
       - `genreEvents: (genre: string) => \`events:genre:${genre.toLowerCase()}\``

    2. **Add methods to EventService.ts** (do NOT modify existing getNearbyEvents -- it's used by check-in auto-suggest):

       a) `getNearbyUpcoming(lat, lon, radiusKm=50, days=30, limit=20)`: Same Haversine pattern as existing getNearbyEvents but with `WHERE e.event_date BETWEEN CURRENT_DATE AND CURRENT_DATE + INTERVAL '${days} days'` instead of `= CURRENT_DATE`. Add bounding box pre-filter for performance: `WHERE v.latitude BETWEEN ($1 - $3/111.0) AND ($1 + $3/111.0) AND v.longitude BETWEEN ($2 - $3/(111.0 * cos(radians($1)))) AND ($2 + $3/(111.0 * cos(radians($1))))`. Use cache.getOrSet with CacheKeys.nearbyEvents, 300s TTL. Returns events with distanceKm, sorted by event_date ASC then distance ASC. Use mapDbEventsWithHeadliner for lineup hydration.

       b) `getTrendingNearby(lat, lon, radiusKm=50, days=7, limit=20)`: Subquery counts check-ins in last `days` days per event. Same Haversine + bounding box. Filter upcoming events (event_date >= CURRENT_DATE, event_date <= CURRENT_DATE + 30 days). Order by recent_checkins DESC, distance_km ASC. Use cache.getOrSet with CacheKeys.trendingEvents, 300s TTL. Use mapDbEventsWithHeadliner.

       c) `getByGenre(genre, limit=20, offset=0)`: Join events -> event_lineup -> bands. `WHERE b.genre ILIKE '%' || $1 || '%' AND e.event_date >= CURRENT_DATE AND e.is_cancelled = FALSE`. Group by e.id, order by event_date ASC. Use cache.getOrSet with CacheKeys.genreEvents, 300s TTL. Use mapDbEventsWithHeadliner.

       d) `searchEvents(query, limit=20)`: Multi-entity event search using pg_trgm. SQL:
          ```sql
          SELECT DISTINCT ON (e.id) e.*, v.name as venue_name, v.city as venue_city, v.state as venue_state, v.image_url as venue_image,
            (SELECT COUNT(*) FROM checkins c WHERE c.event_id = e.id) as checkin_count,
            GREATEST(
              similarity(COALESCE(e.event_name,''), $1),
              similarity(v.name, $1)
            ) as relevance
          FROM events e
          JOIN venues v ON e.venue_id = v.id
          LEFT JOIN event_lineup el ON e.id = el.event_id
          LEFT JOIN bands b ON el.band_id = b.id
          WHERE e.event_date >= CURRENT_DATE AND e.is_cancelled = FALSE
            AND (
              COALESCE(e.event_name,'') % $1
              OR v.name % $1
              OR b.name % $1
              OR b.genre ILIKE '%' || $1 || '%'
            )
          ORDER BY e.id, relevance DESC
          ```
          Then wrap: `SELECT * FROM (...) sub ORDER BY relevance DESC LIMIT $2`. Use pg_trgm similarity threshold 0.2 via `SET pg_trgm.similarity_threshold = 0.2` at session/connection level. Use mapDbEventsWithHeadliner.

    3. **Add controller methods to EventController.ts**:

       a) `getNearbyUpcoming`: GET /api/events/discover?lat=&lon=&radius=&days=&limit=. Validate lat/lon required. Parse radius (default 50), days (default 30), limit (default 20). Calls eventService.getNearbyUpcoming().

       b) `getTrendingNearby`: GET /api/events/trending -- ENHANCE existing handler. Add optional lat/lon query params. If lat/lon provided, call eventService.getTrendingNearby(lat, lon, radius). If not provided, fall back to existing eventService.getTrendingEvents(limit) for backward compat.

       c) `getByGenre`: GET /api/events/genre/:genre?limit=&offset=. Validate genre param. Calls eventService.getByGenre().

       d) `searchEvents`: GET /api/events/search?q=&limit=. Validate q required. Calls eventService.searchEvents().

    4. **Add routes to eventRoutes.ts**: Add BEFORE the `/:id` route (to avoid param conflict):
       - `router.get('/discover', authenticateToken, eventController.getNearbyUpcoming);`
       - `router.get('/genre/:genre', eventController.getByGenre);`
       - `router.get('/search', eventController.searchEvents);`
       Note: /trending already exists, just enhance its handler.

    5. **Extend search routes** (`searchRoutes.ts`): Add event search alongside user search:
       - `router.get('/events', searchRateLimit, eventController.searchEvents);`
       This gives both /api/events/search and /api/search/events paths for discoverability.
  </action>
  <verify>
    - `npx tsc --noEmit` compiles without errors
    - EventService.ts has getNearbyUpcoming, getTrendingNearby, getByGenre, searchEvents methods
    - eventRoutes.ts has /discover, /genre/:genre, /search routes before /:id
    - CacheKeys includes nearbyEvents, trendingEvents, genreEvents
    - Existing getNearbyEvents method is UNCHANGED (still used by check-in flow)
    - Existing /trending route backward-compatible (works without lat/lon)
  </verify>
  <done>
    Five new/enhanced event discovery endpoints operational: GET /api/events/discover (nearby upcoming), GET /api/events/trending (enhanced with location), GET /api/events/genre/:genre, GET /api/events/search, GET /api/search/events. All use Redis cache with 300s TTL. Existing endpoints unchanged for backward compat.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile -- Discovery models, repository, providers, and enhanced discover screen</name>
  <files>
    mobile/lib/src/features/discover/domain/discovery_models.dart
    mobile/lib/src/features/discover/data/discovery_repository.dart
    mobile/lib/src/features/discover/presentation/providers/discover_providers.dart
    mobile/lib/src/features/discover/presentation/discover_screen.dart
  </files>
  <action>
    1. **Create directory structure**: Ensure `mobile/lib/src/features/discover/domain/` and `mobile/lib/src/features/discover/data/` directories exist.

    2. **Create discovery_models.dart** (Freezed models):
       ```dart
       @freezed sealed class DiscoverEvent with _$DiscoverEvent {
         const factory DiscoverEvent({
           required String id,
           required String eventDate,
           String? eventName,
           String? venueName,
           String? venueCity,
           String? venueState,
           double? distanceKm,
           @Default(0) int checkinCount,
           // Lineup info (headliner)
           String? bandName,
           String? bandGenre,
           String? bandImageUrl,
         }) = _DiscoverEvent;
         factory DiscoverEvent.fromJson(Map<String, dynamic> json) => _$DiscoverEventFromJson(json);
       }
       ```
       Parse the API response: the response includes nested `venue: { name, city, state }`, `lineup: [{ band: { name, genre } }]`, and `distanceKm`. Write a custom fromJson or a factory that extracts headliner from lineup[0].band. The backend returns Event objects, so map: eventName from event_name or headliner band name, venueName from venue.name, bandName from lineup[0].band.name.

    3. **Create discovery_repository.dart**: Class `DiscoveryRepository` with DioClient (follow band_repository.dart pattern):
       - `getNearbyUpcoming({lat, lon, radiusKm, days, limit})` -> GET /api/events/discover
       - `getTrendingNearby({lat, lon, radiusKm, limit})` -> GET /api/events/trending?lat=&lon=&radius=
       - `getEventsByGenre({genre, limit, offset})` -> GET /api/events/genre/$genre
       - `searchEvents({query, limit})` -> GET /api/events/search?q=
       - `getGenres()` -> GET /api/bands/genres (existing endpoint)
       All methods parse response.data['data'] list into DiscoverEvent objects. Handle the Event response shape from backend (nested venue/lineup objects).
       Register `discoveryRepositoryProvider` as a keepAlive Riverpod provider (same pattern as profileRepositoryProvider in Phase 6).

    4. **Update discover_providers.dart**: Add new providers:
       - `nearbyUpcomingEventsProvider`: FutureProvider that watches currentLocationProvider. If location available, calls discoveryRepository.getNearbyUpcoming(). Returns List<DiscoverEvent>.
       - `trendingNearbyEventsProvider`: Same location pattern, calls getTrendingNearby().
       - `genreListProvider`: FutureProvider that calls bandRepository.getGenres() (existing). Returns List<String>.
       - `genreEventsProvider(String genre)`: Family provider that calls getEventsByGenre().
       - `discoverEventSearchProvider`: watches discoverSearchQueryProvider, calls searchEvents() when query.length >= 2.
       - Update `discoverSearchResultsProvider` to also include event search results. Add `List<DiscoverEvent> events` field to DiscoverSearchResults class. Watch discoverEventSearchProvider alongside existing band/venue/user searches.

    5. **Enhance discover_screen.dart** `_buildTrendingContent()`: Replace the current band/venue-centric layout with event-first discovery:

       a) **Nearby Shows section** (replaces "Nearby Venues"): Horizontal scrollable list of event cards showing: event name or headliner band name, venue name + city, date, distance. Uses nearbyUpcomingEventsProvider. Same location permission handling as existing nearbyVenues section. Card taps navigate to event detail (if route exists) or show event info.

       b) **Genre Browse section** (NEW): Horizontal scrollable list of genre chips/pills. Uses genreListProvider. Tapping a genre chip shows a modal bottom sheet or inline expansion with events for that genre from genreEventsProvider.

       c) **Trending Near You section** (replaces "Trending Locally"): Horizontal list of event cards with check-in count badge. Uses trendingNearbyEventsProvider.

       d) Keep "Popular This Week" bands section at the bottom as secondary content.

       e) **Enhance search results**: Add "Events" section to _buildSearchResults(). When search results contain events, show them with calendar icon, event name, venue, date. Follows existing _SearchResultItem pattern but with SearchResultType.event added to the enum.

       Style: Use AppTheme.cardDark, electricPurple accents, neonPink for fire/trending. Event cards: 160px width, show date prominently (month/day), venue name, headliner. Genre chips: pill-shaped with genre text, AppTheme.surfaceVariantDark background, electricPurple when selected.

    6. **Run Freezed codegen**: `dart run build_runner build --delete-conflicting-outputs`
  </action>
  <verify>
    - `dart run build_runner build --delete-conflicting-outputs` completes from mobile/ directory
    - `dart analyze lib/` shows no new errors
    - discovery_models.dart contains DiscoverEvent Freezed class
    - discovery_repository.dart contains DiscoveryRepository with 4+ methods
    - discover_providers.dart has nearbyUpcomingEventsProvider, trendingNearbyEventsProvider, genreListProvider
    - discover_screen.dart references "Nearby Shows" section
    - DiscoverSearchResults class includes events field
  </verify>
  <done>
    Discover screen shows event-first layout: Nearby Shows (GPS-based), Genre Browse (chip filters), Trending Near You (check-in popularity). Search results include events alongside bands, venues, users. All data sourced from new backend discovery endpoints via DiscoveryRepository. Freezed codegen passes clean.
  </done>
</task>

</tasks>

<verification>
- Backend compiles: `npx tsc --noEmit` from backend/ directory
- Mobile codegen: `dart run build_runner build --delete-conflicting-outputs` from mobile/ directory
- Mobile analysis: `dart analyze lib/` from mobile/ directory with no new errors
- GET /api/events/discover?lat=40&lon=-74&radius=50 returns upcoming nearby events
- GET /api/events/trending?lat=40&lon=-74 returns trending events with check-in counts
- GET /api/events/genre/rock returns rock events sorted by date
- GET /api/events/search?q=metallica returns matching events
- Existing GET /api/events/nearby (check-in flow) still returns today-only events
- Existing GET /api/events/trending (no lat/lon) still works for backward compat
- Discover screen shows Nearby Shows, Genre Browse, Trending sections
</verification>

<success_criteria>
1. Upcoming nearby shows displayed based on GPS with configurable radius (default 50km) and date range (default 30 days)
2. Genre browse returns events filtered by genre through event_lineup -> bands join
3. Trending near user sorts by recent check-in count within radius
4. Event search matches by event name, band name, venue name, or genre using pg_trgm
5. Discover screen pivots to event-first with Nearby Shows, Genre Browse, Trending sections
6. Search results include events section alongside bands, venues, users
7. All discovery endpoints cached in Redis with 300s TTL
</success_criteria>

<output>
After completion, create `.planning/phases/07-discovery-recommendations/07-02-SUMMARY.md`
</output>
