---
phase: 16-operational-readiness
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - backend/.env.example
  - backend/DEPLOYMENT.md
autonomous: true
requirements:
  - BETA-20
  - BETA-21
  - BETA-22
  - BETA-24
must_haves:
  truths:
    - "DEPLOYMENT.md contains Railway-specific deployment runbook with pre-deploy checklist, deploy steps, post-deploy smoke test procedure, and rollback procedure"
    - "DEPLOYMENT.md documents staging environment setup: Railway project clone, separate DATABASE_URL, environment variable mirroring"
    - "DEPLOYMENT.md documents uptime monitoring setup with UptimeRobot free tier targeting GET /health"
    - "DEPLOYMENT.md documents Sentry DSN configuration steps for Railway"
    - ".env.example SENTRY_DSN entry has clear setup instructions"
    - "DEPLOYMENT.md does not reference Vercel, Heroku, or Render — those are removed as outdated"
    - "DEPLOYMENT.md does not recommend CORS_ORIGIN=* for production"
  artifacts:
    - path: "backend/DEPLOYMENT.md"
      provides: "production deployment runbook for Railway"
      contains: "Railway"
    - path: "backend/.env.example"
      provides: "updated environment variable documentation"
      contains: "SENTRY_DSN"
  key_links:
    - from: "backend/DEPLOYMENT.md:staging"
      to: "railway.toml"
      via: "Railway project configuration"
      pattern: "staging"
    - from: "backend/DEPLOYMENT.md:sentry"
      to: "backend/src/utils/sentry.ts"
      via: "SENTRY_DSN environment variable"
      pattern: "SENTRY_DSN"
---

<objective>
Write the operational documentation and configuration guides needed to run SoundCheck as a live beta.

Purpose: Provide a complete deployment runbook (deploy, verify, rollback), document staging environment setup on Railway, document uptime monitoring with UptimeRobot, and document Sentry DSN configuration. Replace the outdated generic DEPLOYMENT.md with Railway-specific operational procedures.

Output: Rewritten backend/DEPLOYMENT.md, updated backend/.env.example with clearer Sentry instructions.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v2.0-ROADMAP.md
@.planning/phases/16-operational-readiness/RESEARCH-ops.md

<interfaces>
From railway.toml (active deployment config):
```toml
[build]
builder = "NIXPACKS"
watchPaths = ["backend/**"]

[deploy]
startCommand = "cd backend && npm run migrate:up && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
```

From nixpacks.toml (build config — after Plan 01 cleanup, no [start] section):
```toml
[variables]
NIXPACKS_PATH = "./backend"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["(cd backend && npm ci --production=false)"]

[phases.build]
cmds = ["(cd backend && rm -rf dist && npm run build)"]
```

From backend/.env.example (Sentry entry, line 85):
```
# Sentry DSN for error tracking (optional)
SENTRY_DSN=
```

From backend/.env.example (Railway checklist, lines 99-126):
```
## Railway Environment Variables Checklist
# Copy these to Railway Dashboard > Variables for your service:
# [ ] DATABASE_URL (auto-provisioned by Railway Postgres plugin)
# [ ] JWT_SECRET (generate: openssl rand -hex 32)
# ... (17 items total)
```

From backend/src/index.ts (health endpoint):
```
GET /health → { status, timestamp, version, database, websocket }
Returns 200 on healthy, 503 on failure
```

From backend/src/index.ts (Sentry test route — added by Plan 01):
```
GET /api/debug/sentry-test → throws test error (admin-only)
```

Current DEPLOYMENT.md issues:
- References Vercel, Heroku, Render (not used)
- Recommends CORS_ORIGIN=* for production (contradicts Phase 13 security hardening)
- Generic steps not specific to Railway monorepo setup
- No staging environment docs
- No rollback procedure
- No uptime monitoring docs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DEPLOYMENT.md as Railway operations runbook</name>
  <files>
    backend/DEPLOYMENT.md
  </files>
  <action>
**Replace the entire contents of backend/DEPLOYMENT.md** with a Railway-focused operational runbook. The document should be structured as follows:

```markdown
# SoundCheck Deployment & Operations Runbook

> Railway-based deployment for the SoundCheck backend API.

## Architecture

- **Platform**: Railway (Nixpacks builder)
- **Build config**: `nixpacks.toml` (install + build phases)
- **Deploy config**: `railway.toml` (start command with auto-migration)
- **Database**: Railway-provisioned PostgreSQL
- **Cache/Queue**: Railway-provisioned Redis (optional — app degrades gracefully)
- **Monitoring**: Sentry (error tracking) + UptimeRobot (uptime)

## Environment Variables

See `.env.example` for the full list with tiers and descriptions.

**Critical variables that must be set in Railway:**

| Variable | Example | Notes |
|----------|---------|-------|
| `DATABASE_URL` | (auto-provisioned) | Railway Postgres plugin sets this |
| `JWT_SECRET` | `openssl rand -hex 32` | Rotate on any suspected leak |
| `NODE_ENV` | `production` | Must be `production` — controls CORS, error responses, logging |
| `CORS_ORIGIN` | `https://soundcheck.app` | Never use `*` in production |
| `SENTRY_DSN` | `https://xxx@o123.ingest.sentry.io/456` | See [Sentry Setup](#sentry-setup) |
| `REDIS_URL` | (auto-provisioned) | Railway Redis plugin sets this |
| `ENABLE_WEBSOCKET` | `true` | Required for real-time features |

## Deploy Procedure

### Pre-Deploy Checklist

- [ ] All changes committed and pushed to `master`
- [ ] CI pipeline passes (GitHub Actions: build, test, gitleaks)
- [ ] If schema changes: migration file exists in `backend/migrations/`
- [ ] If new env vars: added to Railway Variables dashboard
- [ ] If risky change: tested on staging first (see [Staging](#staging-environment))

### Deploy Steps

1. **Push to master**: Railway auto-deploys on push to `master` via GitHub webhook
2. **Monitor build**: Railway Dashboard → Deployments → watch build logs
3. **Verify startup**: Build completes → migrations run → server starts on configured port
4. **Run smoke check**: After deploy completes, verify core endpoints (see [Post-Deploy Verification](#post-deploy-verification))

### Post-Deploy Verification

Run these checks against the production URL after each deploy:

```bash
# 1. Health check
curl -s https://YOUR_RAILWAY_URL/health | jq .

# Expected: { "success": true, "data": { "status": "healthy", "database": "connected" } }

# 2. Auth endpoint responds
curl -s -X POST https://YOUR_RAILWAY_URL/api/users/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"wrong"}' | jq .status

# Expected: 401 (proves the auth pipeline is working, not that credentials are valid)

# 3. Protected route enforces auth
curl -s https://YOUR_RAILWAY_URL/api/feed | jq .

# Expected: { "success": false, "error": "Access token required" }

# 4. Sentry test (admin only — requires valid admin JWT)
curl -s https://YOUR_RAILWAY_URL/api/debug/sentry-test \
  -H "Authorization: Bearer ADMIN_JWT_TOKEN"

# Expected: 500 response + error appears in Sentry dashboard within 30 seconds
```

### Rollback Procedure

1. **Railway Dashboard** → Deployments → find last known-good deployment
2. Click **Redeploy** on the good deployment — this rolls back code AND re-runs migrations
3. **If migration was destructive**: Migrations are forward-only. Contact the team to write a reverse migration and deploy that forward.
4. **Emergency**: Railway Dashboard → Settings → **Remove deployment** stops the current deployment immediately

> **Note**: `npm run migrate:up` is idempotent — re-running a previously applied migration is safe.

## Sentry Setup

1. Create a Sentry project at https://sentry.io (free tier: 5k errors/month)
2. Choose **Node.js** as the platform
3. Copy the DSN (looks like `https://xxx@o123.ingest.sentry.io/456`)
4. In Railway Dashboard → Variables, add: `SENTRY_DSN=<your DSN>`
5. Redeploy (or wait for next push)
6. Verify: Hit `GET /api/debug/sentry-test` with an admin token — check Sentry dashboard for the test error
7. Verify data scrubbing: The error event should NOT contain `authorization`, `cookie`, or `x-api-key` headers

**What Sentry captures:**
- All 5xx errors (via global error handler)
- Uncaught exceptions and unhandled promise rejections
- User context (id, email, username) on authenticated requests
- Request path, method, and status code

## Uptime Monitoring Setup

1. Create a free account at https://uptimerobot.com
2. Add a new monitor:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: SoundCheck API
   - **URL**: `https://YOUR_RAILWAY_URL/health`
   - **Monitoring Interval**: 5 minutes
3. Configure alerts:
   - **Alert Type**: Email (default) + SMS if available
   - **Alert Contacts**: Add team email addresses
4. The `/health` endpoint returns:
   - `200` with `status: "healthy"` when database is connected
   - `503` with `status: "unhealthy"` when database is down
5. UptimeRobot will alert when it receives a non-2xx response or the endpoint is unreachable

**Optional: Status Page**
UptimeRobot includes a free public status page. Enable it at Monitor → Status Pages if you want a public uptime dashboard for beta testers.

## Staging Environment

### Setup

1. In Railway Dashboard, click **New Project** → **Deploy from GitHub repo**
2. Select the same SoundCheck repository
3. Configure build/deploy:
   - Railway will auto-detect `railway.toml` and `nixpacks.toml`
   - Add a PostgreSQL plugin (separate from production)
   - Add a Redis plugin (optional)
4. Set environment variables:
   - Copy all production variables from the production service
   - **Change**: `DATABASE_URL` → auto-set by the new Postgres plugin
   - **Change**: `NODE_ENV=staging` (or keep `production` for parity)
   - **Change**: `CORS_ORIGIN` → staging URL
   - **Change**: `SENTRY_DSN` → same Sentry project (use `environment` tag to separate) or a separate project
5. Deploy by pushing to `master` — both production and staging will deploy

### Using Staging

- **Before risky deploys**: Push to a branch, manually deploy from that branch in the staging project
- **Verify migrations**: Staging runs `npm run migrate:up` on the separate database — if migration fails, production is unaffected
- **Test new features**: Use staging URL for manual QA before merging to master

### Cost

Railway free tier: $5/month credit. Staging with Postgres will consume some of this. For a beta with low traffic, one staging service + DB fits within the free tier.

## Monitoring & Alerts Summary

| System | What it monitors | Alert channel | SLA |
|--------|-----------------|---------------|-----|
| Sentry | Backend errors, exceptions | Email + Sentry dashboard | Real-time |
| UptimeRobot | `/health` endpoint availability | Email/SMS | 5-minute check interval |
| Railway | Build failures, deployment status | Railway dashboard + GitHub checks | Per-deploy |
| GitHub Actions | CI pipeline (build, test, secrets) | GitHub notifications | Per-push |

## Troubleshooting

### Logs not visible in Railway

If logs are missing from the Railway dashboard, check that the winston Console transport is present for production (`backend/src/utils/logger.ts`). Production logs must output to stdout for Railway to capture them.

### Migrations fail on deploy

1. Check Railway build logs for the migration error
2. Fix the migration file locally
3. Push to master — Railway will re-run all pending migrations
4. Migrations are idempotent: previously applied migrations are skipped

### Sentry not receiving errors

1. Verify `SENTRY_DSN` is set in Railway Variables
2. Check Railway logs for `Sentry error reporting initialized` on startup
3. Hit the test route: `GET /api/debug/sentry-test` (admin auth required)
4. If behind a CDN or firewall, ensure outbound HTTPS to `*.ingest.sentry.io` is allowed
```
  </action>
  <verify>
    1. `grep "Railway" backend/DEPLOYMENT.md` — Railway-specific content
    2. `grep -c "Vercel\|Heroku\|Render" backend/DEPLOYMENT.md` — returns 0 (no outdated platforms)
    3. `grep -c "CORS_ORIGIN=\*" backend/DEPLOYMENT.md` — returns 0 (no insecure CORS advice)
    4. `grep "staging" backend/DEPLOYMENT.md` — staging environment documented
    5. `grep "rollback" backend/DEPLOYMENT.md` — rollback procedure documented
    6. `grep "UptimeRobot\|uptimerobot" backend/DEPLOYMENT.md` — uptime monitoring documented
    7. `grep "SENTRY_DSN" backend/DEPLOYMENT.md` — Sentry setup documented
  </verify>
  <done>Complete Railway deployment runbook with deploy, verify, rollback, Sentry setup, uptime monitoring, and staging environment procedures.</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example with clearer Sentry and staging guidance</name>
  <files>
    backend/.env.example
  </files>
  <action>
**2a. Update the Sentry DSN entry (around line 85)**

Replace:
```
# Sentry DSN for error tracking (optional)
SENTRY_DSN=
```
With:
```
# Sentry DSN for error tracking (REQUIRED for production)
# Get from: Sentry Dashboard > Settings > Client Keys (DSN)
# Format: https://xxx@o123.ingest.sentry.io/456
# See DEPLOYMENT.md "Sentry Setup" section for full instructions
SENTRY_DSN=
```

**2b. Update the Railway checklist SENTRY_DSN entry (in the checklist around line 99-126)**

Find the `# [ ] SENTRY_DSN` line in the Railway checklist and update it:
```
# [ ] SENTRY_DSN (create project at sentry.io > copy DSN — see DEPLOYMENT.md)
```
  </action>
  <verify>
    1. `grep "REQUIRED for production" backend/.env.example` — upgraded from optional
    2. `grep "DEPLOYMENT.md" backend/.env.example` — cross-reference to runbook
  </verify>
  <done>.env.example updated with clearer Sentry setup instructions and cross-reference to deployment runbook.</done>
</task>

</tasks>

<verification>
1. `backend/DEPLOYMENT.md` exists and contains Railway-specific runbook
2. No references to Vercel, Heroku, or Render in DEPLOYMENT.md
3. No `CORS_ORIGIN=*` recommendation in DEPLOYMENT.md
4. Deployment runbook covers: pre-deploy checklist, deploy steps, post-deploy verification, rollback
5. Staging environment setup is documented with Railway project clone steps
6. Uptime monitoring setup documented (UptimeRobot + /health endpoint)
7. Sentry DSN configuration documented with verification steps
8. `.env.example` has clear Sentry setup instructions
</verification>

<success_criteria>
- BETA-20: Sentry DSN configuration documented with step-by-step setup and verification via test error route
- BETA-21: Uptime monitoring setup documented (UptimeRobot free tier → /health endpoint, 5-minute interval, email/SMS alerts)
- BETA-22: Complete deployment runbook — pre-deploy checklist, deploy steps, post-deploy smoke verification, rollback procedure using Railway dashboard
- BETA-24: Staging environment setup documented — Railway project clone, separate database, environment variable mirroring, branch-based testing workflow
</success_criteria>

<output>
After completion, create `.planning/phases/16-operational-readiness/16-02-SUMMARY.md`
</output>
