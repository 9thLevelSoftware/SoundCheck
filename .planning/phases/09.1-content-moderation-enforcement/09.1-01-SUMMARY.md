---
phase: 09.1-content-moderation-enforcement
plan: 01
subsystem: database, api
tags: [postgresql, content-moderation, is_hidden, partial-index, feed-queries]

# Dependency graph
requires:
  - phase: 09-trust-and-safety
    provides: "is_hidden columns on checkins and checkin_comments (migration 030), ModerationService.autoHideContent()"
provides:
  - "is_hidden filtering on all 8 content-serving queries (FeedService, CheckinQueryService, CheckinToastService)"
  - "Partial indexes idx_checkins_hidden and idx_comments_hidden for query planner optimization"
affects: [content-moderation-enforcement, admin-dashboard, feed-performance]

# Tech tracking
tech-stack:
  added: []
  patterns: ["IS NOT TRUE defensive null-safe boolean filtering", "Partial indexes for soft-delete-like filtering"]

key-files:
  created:
    - backend/migrations/031_add-is-hidden-indexes.ts
  modified:
    - backend/src/services/FeedService.ts
    - backend/src/services/checkin/CheckinQueryService.ts
    - backend/src/services/checkin/CheckinToastService.ts

key-decisions:
  - "Used IS NOT TRUE instead of = FALSE for null-safe filtering of is_hidden column"
  - "Used plain CREATE INDEX (not CONCURRENTLY) since no existing migrations use CONCURRENTLY and it works within transaction blocks"
  - "Separate pgm.sql() calls per index for compatibility with migration runner"

patterns-established:
  - "is_hidden IS NOT TRUE: Standard pattern for filtering hidden content in all SQL queries"
  - "Comment JOIN filtering: LEFT JOIN checkin_comments cm ON ... AND cm.is_hidden IS NOT TRUE"

requirements-completed: [SAFE-02, SAFE-03]

# Metrics
duration: 2min
completed: 2026-02-27
---

# Phase 9.1 Plan 01: Feed Query is_hidden Filtering Summary

**is_hidden content filtering added to all 8 content-serving queries across FeedService, CheckinQueryService, and CheckinToastService with partial indexes for performance**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-27T21:42:35Z
- **Completed:** 2026-02-27T21:44:47Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created migration 031 with partial indexes on checkins and checkin_comments for is_hidden = true rows
- Added is_hidden filtering to all 4 FeedService queries (getFriendsFeed, getEventFeed, getHappeningNow, getUnseenCounts)
- Added is_hidden filtering to all 3 CheckinQueryService queries (getCheckinById, getActivityFeed, getCheckins)
- Added is_hidden filtering to CheckinToastService.getComments
- Fixed comment LEFT JOINs in 5 queries to exclude hidden comments from counts

## Task Commits

Each task was committed atomically:

1. **Task 1: Create partial indexes migration and add is_hidden filter to FeedService** - `8e85167` (feat)
2. **Task 2: Add is_hidden filter to CheckinQueryService and CheckinToastService** - `aec5ad7` (feat)

## Files Created/Modified
- `backend/migrations/031_add-is-hidden-indexes.ts` - Partial indexes for is_hidden = true on checkins and checkin_comments
- `backend/src/services/FeedService.ts` - is_hidden filtering on getFriendsFeed, getEventFeed, getHappeningNow, getUnseenCounts + comment JOIN fixes
- `backend/src/services/checkin/CheckinQueryService.ts` - is_hidden filtering on getCheckinById, getActivityFeed, getCheckins + comment JOIN fixes
- `backend/src/services/checkin/CheckinToastService.ts` - is_hidden filtering on getComments

## Decisions Made
- Used `IS NOT TRUE` instead of `= FALSE` for null-safe filtering (handles potential NULL values defensively)
- Used plain `CREATE INDEX IF NOT EXISTS` instead of `CREATE INDEX CONCURRENTLY` since no existing migrations use CONCURRENTLY and it works correctly within migration runner transaction blocks
- Placed is_hidden filter after table-specific conditions and before block filter for consistent ordering across all queries

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All content-serving queries now filter hidden content
- Ready for Plan 02 (admin moderation endpoints, if applicable)
- ModerationService.autoHideContent() writes are now enforced by read-side filtering

## Self-Check: PASSED

- All 4 files verified to exist on disk
- Both task commits (8e85167, aec5ad7) verified in git log

---
*Phase: 09.1-content-moderation-enforcement*
*Completed: 2026-02-27*
