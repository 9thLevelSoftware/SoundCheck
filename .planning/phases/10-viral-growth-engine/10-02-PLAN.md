---
phase: 10-viral-growth-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/ShareCardService.ts
  - backend/src/templates/share-cards/checkin-card.tsx
  - backend/src/templates/share-cards/badge-card.tsx
  - backend/src/templates/share-cards/landing-page.html
  - backend/src/controllers/ShareController.ts
  - backend/src/routes/shareRoutes.ts
  - backend/src/index.ts
  - backend/package.json
autonomous: true
requirements: [SHARE-01, SHARE-04]
must_haves:
  truths:
    - "Server generates check-in card images in two variants (1200x630 OG and 1080x1920 Stories)"
    - "Server generates badge unlock card images in same two variants"
    - "Generated card images are uploaded to R2 and accessible via public URL"
    - "Non-users clicking a shared link see a web landing page with the card image and App Store/Play Store CTAs"
    - "Landing page HTML includes correct OG meta tags for social platform previews"
  artifacts:
    - path: "backend/src/services/ShareCardService.ts"
      provides: "Satori + resvg-js card generation pipeline"
      exports: ["ShareCardService"]
    - path: "backend/src/templates/share-cards/checkin-card.tsx"
      provides: "Satori JSX template for check-in cards"
      exports: ["checkinCardOG", "checkinCardStories"]
    - path: "backend/src/templates/share-cards/badge-card.tsx"
      provides: "Satori JSX template for badge cards"
      exports: ["badgeCardOG", "badgeCardStories"]
    - path: "backend/src/controllers/ShareController.ts"
      provides: "Card generation and landing page endpoints"
      exports: ["ShareController"]
  key_links:
    - from: "backend/src/services/ShareCardService.ts"
      to: "backend/src/services/R2Service.ts"
      via: "Upload generated PNG to R2"
      pattern: "r2Service"
    - from: "backend/src/controllers/ShareController.ts"
      to: "backend/src/services/ShareCardService.ts"
      via: "controller -> service"
      pattern: "shareCardService"
    - from: "backend/src/routes/shareRoutes.ts"
      to: "landing-page.html"
      via: "Express serves HTML with OG meta tags"
      pattern: "renderCheckinLanding"
---

<objective>
Build the server-side share card image generation pipeline using Satori + @resvg/resvg-js and the web landing page for shared links.

Purpose: Social sharing requires server-generated branded card images (consistent across platforms, work as OG images for link previews). This plan creates the complete image generation pipeline, card templates for check-ins and badges, and the minimal landing page that non-users see when clicking shared links.

Output: ShareCardService that generates two image variants per card, uploads to R2, and returns public URLs. Web landing page with OG meta tags at /share/c/:checkinId and /share/b/:badgeAwardId.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-viral-growth-engine/10-RESEARCH.md

@backend/src/services/R2Service.ts (existing R2 upload infrastructure)
@backend/src/services/CheckinService.ts (for fetching checkin data)
@backend/src/services/BadgeService.ts (for fetching badge award data)
@backend/src/index.ts (route mounting)
@backend/src/types/index.ts (shared types)

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/src/services/R2Service.ts:
```typescript
export class R2Service {
  private s3: S3Client | null = null;
  private bucket: string;
  private publicUrl: string;
  private isConfigured: boolean;
  // Existing: presigned upload for client-side uploads
  // We need: direct upload method for server-generated images
}
export const r2Service: R2Service; // singleton
```

NOTE: R2Service currently only supports presigned URL uploads (client PUT). ShareCardService needs a direct upload method. Either:
(a) Add a `uploadBuffer(buffer: Buffer, key: string, contentType: string): Promise<string>` method to R2Service, OR
(b) Use PutObjectCommand directly in ShareCardService using the existing S3Client config.
Option (a) is cleaner -- add the method to R2Service.

From backend/src/types/index.ts:
```typescript
export interface ApiResponse {
  success: boolean;
  data?: any;
  error?: string;
  message?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install satori + resvg-js, create ShareCardService and card templates</name>
  <files>
    backend/package.json
    backend/src/services/ShareCardService.ts
    backend/src/services/R2Service.ts
    backend/src/templates/share-cards/checkin-card.tsx
    backend/src/templates/share-cards/badge-card.tsx
  </files>
  <action>
    **Install dependencies:**
    ```bash
    cd backend && npm install satori @resvg/resvg-js
    ```

    **Add uploadBuffer to R2Service:**
    Add a method to R2Service that uploads a Buffer directly (for server-generated images):
    ```typescript
    async uploadBuffer(buffer: Buffer, key: string, contentType: string): Promise<string> {
      if (!this.isConfigured || !this.s3) {
        throw new Error('R2 is not configured');
      }
      await this.s3.send(new PutObjectCommand({
        Bucket: this.bucket,
        Key: key,
        Body: buffer,
        ContentType: contentType,
      }));
      return `${this.publicUrl}/${key}`;
    }
    ```

    **Card Templates (TSX files):**
    NOTE: These are Satori JSX templates, not React components. They return JSX elements that Satori renders to SVG. The project uses TypeScript with JSX support -- ensure tsconfig.json allows JSX in these template files (jsx: "react-jsx" or "react").

    checkin-card.tsx: Export two functions:
    - `checkinCardOG(data: CheckinCardData)` -- Returns JSX element at 1200x630. Layout: dark background (#0D0D0D), SoundCheck branding top-left in purple (#A855F7), band name large white text, venue + city in gray, date in gray, "Checked in by @username" at bottom. Rating shown as stars or numeric. All styles inline using flexbox only (no CSS grid, no position:absolute -- Satori limitation).
    - `checkinCardStories(data: CheckinCardData)` -- Same content at 1080x1920 (9:16 Stories ratio). Vertically centered, more spacing.
    - Define `CheckinCardData` interface: { username, bandName, venueName, venueCity, eventDate, rating, bandImageUrl? }

    badge-card.tsx: Export two functions:
    - `badgeCardOG(data: BadgeCardData)` -- 1200x630. Dark background, badge icon/name prominent, "Unlocked by @username", badge description, SoundCheck branding.
    - `badgeCardStories(data: BadgeCardData)` -- 1080x1920 variant.
    - Define `BadgeCardData` interface: { username, badgeName, badgeDescription, badgeCategory, unlockedAt }

    **ShareCardService (new file):**
    ```typescript
    import satori from 'satori';
    import { Resvg } from '@resvg/resvg-js';
    import fs from 'fs';
    import path from 'path';
    import { r2Service } from './R2Service';
    ```

    Load font once at module level: `const fontData = fs.readFileSync(path.join(__dirname, '../fonts/Inter-Bold.ttf'));`
    NOTE: Download Inter-Bold.ttf from Google Fonts and place in backend/src/fonts/. It's OFL licensed.

    Create `backend/src/fonts/` directory and include Inter-Bold.ttf (use curl or a build step to download).

    Methods:
    - `async generateCheckinCard(data: CheckinCardData): Promise<{ ogUrl: string; storiesUrl: string }>` -- Uses satori() with checkinCardOG template at {width:1200, height:630}, then Resvg to render to PNG. Same for stories variant at {width:1080, height:1920}. Upload both to R2 at `cards/checkin/${checkinId}-og.png` and `cards/checkin/${checkinId}-stories.png`. Use content-addressable paths (include a timestamp or short hash) to avoid OG image cache staleness.
    - `async generateBadgeCard(data: BadgeCardData): Promise<{ ogUrl: string; storiesUrl: string }>` -- Same pattern with badge templates. Upload to `cards/badge/${badgeAwardId}-og.png`.
    - Private helper `async renderAndUpload(jsx, width, height, key): Promise<string>` -- DRY helper for satori -> resvg -> R2 upload.
    - Graceful degradation: If R2 is not configured, log a warning and return placeholder URLs (empty strings). Do NOT throw -- sharing should degrade gracefully.

    Font handling: Satori requires font as ArrayBuffer. Use `fontData.buffer.slice(fontData.byteOffset, fontData.byteOffset + fontData.byteLength)` to convert Node Buffer to ArrayBuffer.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit src/services/ShareCardService.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    satori and @resvg/resvg-js installed. R2Service has uploadBuffer method. ShareCardService generates check-in and badge card images in two variants (OG + Stories), uploads to R2, returns public URLs. Card templates use Satori-compatible flexbox-only JSX. Font file included. Graceful degradation when R2 unconfigured.
  </done>
</task>

<task type="auto">
  <name>Task 2: ShareController, routes, landing page, and route mounting</name>
  <files>
    backend/src/controllers/ShareController.ts
    backend/src/routes/shareRoutes.ts
    backend/src/templates/share-cards/landing-page.html
    backend/src/index.ts
  </files>
  <action>
    **Landing page template (landing-page.html):**
    A minimal HTML template with placeholder tokens ({{TITLE}}, {{DESCRIPTION}}, {{IMAGE_URL}}, {{PAGE_URL}}, {{APP_STORE_URL}}, {{PLAY_STORE_URL}}, {{CARD_TYPE}}) that gets string-replaced at serve time. NOT a full SPA -- just static HTML.

    Content:
    - `<head>`: OG meta tags (og:title, og:description, og:image, og:image:width=1200, og:image:height=630, og:url, og:type=website), Twitter card tags (twitter:card=summary_large_image, twitter:image, twitter:title, twitter:description).
    - `<body>`: Centered card preview image, SoundCheck branding, "Download SoundCheck" heading, App Store badge (link from env var APP_STORE_URL or placeholder), Play Store badge (link from env var PLAY_STORE_URL or placeholder). Clean dark theme matching app branding. Inline CSS only -- no external stylesheets.

    Use HTML entities for user-generated content (XSS prevention). ShareController must sanitize username, bandName, venueName before injecting into template.

    **ShareController (new file):**
    Follow existing controller pattern:
    - Constructor takes ShareCardService, CheckinService, BadgeService
    - `generateCheckinCard = async (req, res)` -- POST /api/share/checkin/:checkinId -- Fetch checkin data (with band, venue, user info), call shareCardService.generateCheckinCard(), return { ogUrl, storiesUrl }.
    - `generateBadgeCard = async (req, res)` -- POST /api/share/badge/:badgeAwardId -- Fetch badge award data, call shareCardService.generateBadgeCard(), return { ogUrl, storiesUrl }.
    - `renderCheckinLanding = async (req, res)` -- GET /share/c/:checkinId -- Fetch checkin data, get card image URL (generate if not exists), read landing-page.html template, replace tokens, send HTML response (res.send, NOT res.json). Set Content-Type to text/html.
    - `renderBadgeLanding = async (req, res)` -- GET /share/b/:badgeAwardId -- Same pattern for badge.

    HTML sanitization helper: Create a simple `escapeHtml(str)` function that replaces &, <, >, ", ' with HTML entities. Apply to ALL user-generated strings before template injection.

    **Routes (shareRoutes.ts):**
    - POST /api/share/checkin/:checkinId -- requires authenticateToken -- generates card
    - POST /api/share/badge/:badgeAwardId -- requires authenticateToken -- generates card
    - GET /share/c/:checkinId -- NO auth required (public landing page for non-users)
    - GET /share/b/:badgeAwardId -- NO auth required (public landing page)

    IMPORTANT: The landing page routes (/share/c/ and /share/b/) are NOT under /api/ prefix. They are public web pages. Mount them separately in index.ts.

    **index.ts modifications:**
    ```typescript
    import shareRoutes from './routes/shareRoutes';
    // API routes (authenticated)
    app.use('/api/share', shareRoutes.api);
    // Public landing pages (no auth)
    app.use('/share', shareRoutes.public);
    ```

    Alternative: Export two routers from shareRoutes.ts (apiRouter and publicRouter), or mount all in one router and let route-level middleware handle auth selectively.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    ShareController generates card images via API endpoints and serves landing pages with correct OG meta tags. Landing page template renders card preview, SoundCheck branding, and store CTAs. Public routes at /share/c/:id and /share/b/:id serve HTML to social platform crawlers and non-users. API routes at /api/share/* require authentication. HTML output is XSS-safe via escapeHtml. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd backend && npx tsc --noEmit`
2. satori and @resvg/resvg-js in package.json dependencies
3. Card templates use only flexbox (no CSS grid, no position:absolute)
4. Landing page HTML includes og:image, og:title, twitter:card meta tags
5. Public landing routes do NOT require authentication
6. API card generation routes DO require authentication
7. User-generated content in HTML is escaped via escapeHtml
</verification>

<success_criteria>
- ShareCardService generates PNG images from JSX templates via satori + resvg-js pipeline
- Two variants per card: 1200x630 (OG) and 1080x1920 (Stories)
- Card images uploaded to R2 with content-addressable URLs
- Landing page at /share/c/:checkinId shows card preview + store CTAs with correct OG tags
- Landing page at /share/b/:badgeAwardId shows badge card preview + store CTAs
- Graceful degradation when R2 is not configured
- No XSS vulnerabilities in landing page HTML
</success_criteria>

<output>
After completion, create `.planning/phases/10-viral-growth-engine/10-02-SUMMARY.md`
</output>
